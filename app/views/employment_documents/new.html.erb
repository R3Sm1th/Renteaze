<%# <%= <h1>New Employment Form</h1> %>

<%= render "form", employment_document: @employment_document %>

<%# <div class="container">
  <%= simple_form_for [@property_application, @employment_document] do |f| %>
    <%# <%= f.input :company_name %>
    <%# <%= f.input :date %>
    <%# <%= f.input :status %>
    <%# <%= f.input :pdf, as: :file %>
    <%# <%= f.button :submit, class: "btn btn-primary" %>
  <%# <% end %>
<%# </div> %>

<main class="container">
  <%= link_to sanitize("&larr; Back to Dashboard"), property_application_path(params[:property_application_id]) %>

  <%# <%= turbo_frame_tag "second_frame" do %>
  <div class="header">
    <h1>New Employment Reference</h1>
  </div>

  <%= turbo_frame_tag "new_e" do %>
    <%= render "form", employment_document: @employment_document %>
    <%= link_to 'Cancel', property_application_path(@property_application) %>
  <% end %>

 <%# NEW IDEA %>
    <%= turbo_frame_tag :application_step do %>
      <%= render 'property_applications/steps', current_step: 2, property_application: @property_application %>
      <%= render "form", identifications: @identification %>
      <%= link_to 'Cancel', tenant_index_property_application_employment_documents_path(@property_application) %>
    <% end %>
  <%# END NEW IDEA %>

</main>
